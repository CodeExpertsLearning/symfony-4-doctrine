{% extends 'base.html.twig' %}

{% block body %}
    <h1>Usuarios</h1>
    <ul>
        {% for u in users %}
        <li>
            {{ u.name }} | {{ u.email }}
        </li>
        {% endfor %}
    </ul>
{% endblock %}